import 'package:flutter/material.dart';

class GqlNoData extends StatelessWidget {
  const GqlNoData({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No Data', style: TextStyle(color: Colors.white)),
    );
  }
}
